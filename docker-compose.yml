version: '3.8'

services:
  flask-api:
    build: .
    ports:
      - "3002:3002"
    env_file:
      - .env
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - NEWS_API_KEY=${NEWS_API_KEY}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
    volumes:
      - .:/app